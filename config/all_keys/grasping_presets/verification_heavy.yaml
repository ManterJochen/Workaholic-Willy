# ============================================================================
# grasping_presets/verification_heavy.yaml -- a mode preset, copied verbatim
# ============================================================================

# Phase preset: verification-heavy closed loop with recovery enabled.
grasping:
  default_mode: closed_loop
  recovery:
    enabled: true
    allowed_actions:
      - next_viewpoint
